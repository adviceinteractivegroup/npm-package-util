'use strict';
const assert = require('chai').assert;
const {getAddressScore} = require('../lib/addressScore');
const addressList = require('../test/data_mock/addressList');

describe('address Score', function() {
  describe('getAddressScore', function () {
    it('should return a selected score result', function () {
      let result = getAddressScore(addressListnpm);
    });
  });
});